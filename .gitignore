# Maven build output
target/

# Modern UI 前端构建工具（不入 jar）与生成的 dist（构建时自动生成）
oci-server/modern-ui-build/node_modules/
oci-server/src/main/resources/static/modern-ui/dist/

# IDE
.idea/
*.iml
.DS_Store

# Runtime data
data/
logs/

# Environment / secrets（.env.example 保留可提交，其余一律不入库）
.env
.env.*
!.env.example

# Private keys
*.pem
*.key
*.p12
*.jks

# ---------- oci-pool-win (.NET / local build) ----------
oci-pool-win/.build/
oci-pool-win/**/bin/
oci-pool-win/**/obj/
oci-pool-win/**/*.user
oci-pool-win/**/*.suo
*.msi

# ---------- oci-pool-mac (Xcode / local build) ----------
# xcodegen / tool cache
oci-pool-mac/.build/
# xcodebuild -derivedDataPath ./DerivedData（勿提交，体积巨大）
oci-pool-mac/DerivedData/
# 其它本机 DerivedData / 通用 Xcode 缓存
**/DerivedData/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
# 打包产物
*.dmg
*.app/
# 本地进度 / 说明文档（勿提交）
oci-pool-mac/README.md
oci-pool-mac/PROGRESS.md
oci-pool-mac/OciPool/Common/README.md
oci-pool-mac/OciPool/Features/Tenants/README.md

# AI agent local plans / todos / lessons (private, not for remote)
tasks/
plan.md
.plans/
.grok/
.superpowers/
docs/superpowers/

# Claude Code internals
.claude/
.claude/worktrees/

# Claude project instructions (private, not for remote)
CLAUDE.MD
Claude.md

# GeoIP 数据库：构建时通过 MAXMIND_LICENSE_KEY 下载，不提交仓库
*.mmdb
GeoLite2-City*.tar.gz

# 本地 gitleaks 扫描产物
gitleaks.sarif
